{
  "name": "tracks-land",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "mkdir -p dist/images && cp index.html dist/ && cp -r public/images/* dist/images/"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
