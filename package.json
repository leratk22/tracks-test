{
  "name": "tracks-land",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "fonts": "node scripts/copy-fonts.js",
    "build": "npm run fonts 2>/dev/null || true && mkdir -p dist/images dist/fonts && cp index.html dist/ && cp -r public/images/* dist/images/ && (test -d public/fonts && cp -r public/fonts/* dist/fonts/ || true) && OUT_DIR=dist/images node scripts/convert-images.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
